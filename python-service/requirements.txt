# --- Lumina analysis microservice ---
# Version floors (not hard pins) so pip can resolve a consistent set for your
# Python version. Install into a virtualenv — see scripts/setup.* or the README.
# Python 3.10–3.12 recommended (androguard has trouble on 3.13+).

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.7,<3.0

# LLM synthesis & chat — any OpenAI-compatible provider
# (OpenAI, Google Gemini, OpenRouter, or a custom endpoint).
openai>=1.40,<2.0

# Static analysis engine — structural APK parsing (manifest, permissions, call graph)
androguard>=4.1,<5.0

# Threat-intel reputation lookups
requests>=2.31,<3.0

# --- Optional / feature-gated (import guarded in code) ---
#   frida  -> dynamic instrumentation (requires frida-server on the emulator)
#             install manually if you need real dynamic analysis:  pip install frida
